<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp" />
    <!-- 圆角 -->
    <corners android:radius="1dp" /><!-- 设置圆角半径 -->
    <!-- 填充 -->
    <stroke
        android:width="0.6dp"
        android:color="@color/colorPrimary" />
    <solid android:color="@color/colorBackgroundDefault" /><!-- 填充的颜色 -->
</shape>